function Heading({ items }) {
  // // const items = props.items
  // const { items } = props

  console.log(items);

  return (
    <div>
      <h2>Bonjour!</h2>
    </div>
  );
}

export default Heading;
